<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../Partials/head.ejs %>
</head>
<body>
    <% include ../Partials/nav.ejs %>

    <div class="hero">
        <div class="hero-body">
            <h1>Log in</h1>
            <p>From submitting a ticket, to being a support agent, to managing FOSSTicket, signing in completes the experience.</p>
        </div>
    </div>

    <main>
        <% if (notification.display) { %>
            <div class="notification <%= notification.color %>">
                <h3><%= notification.title %></h3>
                <p><%= notification.description %></p>
            </div>
            <br>
        <% } %>

        <form action="/login" method="POST">
            <input type="text" name="username" placeholder="Username" minlength="1"><br><br>
            <input type="password" name="password" placeholder="Password" minlength="8"><br><br>
    
            <input type="submit" value="Log in">
        </form>
    </main>

    <% include ../Partials/footer.ejs %>
</body>
</html>